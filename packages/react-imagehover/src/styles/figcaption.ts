import { css } from '@emotion/core'

export default () =>
  css({
    backgroundColor: '#135796',
    padding: 30,
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    opacity: 0
  })
